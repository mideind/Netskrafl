# This directory holds Google Cloud credentials for local Docker development.
# Files here are ignored by git (see .gitignore).
#
# To use:
#   1. Download a service account key from Google Cloud Console
#   2. Save it as: credentials/service-account.json
#   3. Run: docker-compose up
#
# Alternative: Set GOOGLE_CREDENTIALS_BASE64 environment variable
#   export GOOGLE_CREDENTIALS_BASE64=$(base64 -w0 your-credentials.json)
#   docker-compose up
