[pytest]
markers =
    slow: slow and costly tests, e.g. replays of production games issuing
        thousands of Cloud Datastore round-trips (test/test_replay.py).
        Deselected by default via addopts; run explicitly with -m slow.
addopts = -m "not slow"
